/* Onde está a API (backend FastAPI).
 *
 * - Frontend servido pelo próprio backend (local ou só no Render): deixe ''.
 * - Frontend na Vercel + backend no Render: coloque o endereço do Render, SEM barra no fim:
 *     apiBase: 'https://clearcut-api.onrender.com'
 */
window.APP_CONFIG = {
  apiBase: '',
};
